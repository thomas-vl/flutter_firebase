name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0

publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  equatable: ^2.0.3
  firebase_auth: ^3.1.3
  flutter:
    sdk: flutter
